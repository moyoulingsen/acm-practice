
// 枚举上下边界，把二维矩形压成一维列和数组；
// 再利用列和非负这一性质，使前缀和单调，
// 从而用双指针在线性时间统计和为 K 的列区间数；
// 总复杂度为 O(H^2 * W)。





// 4. 关键转化：先求 >=K 和 >K
// 滑动窗口的关键

// 为什么可以用滑动窗口
// 关键性质
// 因为原矩阵中的数是 0/1，所以：


// A[j] >= 0
// 因此前缀和数组 B 一定是：


// B[0] <= B[1] <= B[2] <= ... <= B[W]
// 即：


// B 单调不减
// 6. 最小合法 r 单调不减
// 现在考虑统计：


// B[r] - B[l] >= K
// 对于固定的 l，设最小满足条件的 r 为 r0。

// 那么：

// r0 满足条件
// 所有 r >= r0 也都满足条件
// 所以当前 l 的贡献为：


// W - r0 + 1