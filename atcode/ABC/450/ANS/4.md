# D - Minimize Range
## 原题（英文）
### Time Limit
2 sec / Memory Limit: 1024 MiB
Score : 400 points

### Problem Statement
You are given a sequence $A$ of $N$ positive integers and a positive integer $K$.
You can perform the following operation on the sequence $A$ any number of times.

Choose an integer $i$ with $1 \le i \le N$, and add $K$ to $A_i$.

Find the minimum possible value of $\max(A) - \min(A)$.

### Constraints
- $1 \le N \le 2 \times 10^5$
- $1 \le K \le 10^9$
- $1 \le A_i \le 10^9$
- All input values are integers.

### Input
The input is given from Standard Input in the following format:
```
N K
A_1 A_2 \dots A_N
```

### Output
Output the answer on a single line.

### Sample Input 1
```
3 10
3 21 9
```

### Sample Output 1
```
4
```
Sample Explanation 1:
First, choosing $i=1$ makes the sequence $A=(13,21,9)$.
Next, choosing $i=3$ makes the sequence $A=(13,21,19)$.
Next, choosing $i=1$ makes the sequence $A=(23,21,19)$.
At this point, $\max(A)-\min(A)=23-19=4$.
It is impossible to make $\max(A)-\min(A)$ at most $3$, so the answer is $4$.

### Sample Input 2
```
5 6
4 100 5 10 450
```

### Sample Output 2
```
2
```

---

## 中文翻译
### 时间限制
2秒 / 内存限制：1024MB
得分：400分

### 问题描述
给定一个由 $N$ 个正整数组成的序列 $A$ 和一个正整数 $K$。
你可以对序列 $A$ 执行**任意次数**以下操作：

选择一个满足 $1 \le i \le N$ 的整数 $i$，并将 $A_i$ 加上 $K$。

请求出 $\max(A) - \min(A)$ 的最小可能值。

### 约束条件
- $1 \le N \le 2 \times 10^5$
- $1 \le K \le 10^9$
- $1 \le A_i \le 10^9$
- 所有输入值均为整数。

### 输入格式
输入从标准输入按以下格式读取：
```
N K
A_1 A_2 \dots A_N
```

### 输出格式
输出一行，包含答案。

### 样例输入1
```
3 10
3 21 9
```

### 样例输出1
```
4
```

### 样例解释1
首先，选择 $i=1$，序列变为 $A=(13,21,9)$。
接着，选择 $i=3$，序列变为 $A=(13,21,19)$。
然后，选择 $i=1$，序列变为 $A=(23,21,19)$。
此时 $\max(A)-\min(A)=23-19=4$。
无法让 $\max(A)-\min(A)$ 小于等于 3，因此答案为 4。

### 样例输入2
```
5 6
4 100 5 10 450
```

### 样例输出2
```
2
```