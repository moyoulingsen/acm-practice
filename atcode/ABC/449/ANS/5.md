# E - A += v
## 原题（英文）
Time Limit: 2 sec / Memory Limit: 1024 MiB
Score : 475 points

### Problem Statement
You are given integers $N,M$ and an integer sequence $A=(A_1,A_2,\dots,A_N)$ of length $N$ where each element is between $1$ and $M$, inclusive.

The following operation is performed $10^{100}$ times on this integer sequence $A$:

Let $v$ be the integer between $1$ and $M$, inclusive, with the fewest occurrences in $A$. If there are multiple such $v$, take the smallest among them. Then, append $v$ to the end of $A$.

You are given $Q$ queries. The $i$-th query gives an integer $X_i$, so find the value of $A_{X_i}$ after performing the operation $10^{100}$ times.

### Constraints
- $1\le N,M\le 5\times 10^5$
- $1\le A_i \le M$
- $1\le Q\le 2\times 10^5$
- $1\le X_i \le 10^{18}$
- All input values are integers.

### Input
The input is given from Standard Input in the following format:
```
N M
A_1 A_2 \dots A_N
Q
X_1
X_2
⋮
X_Q
```

### Output
Output $Q$ lines.
The $i$-th line should contain the answer to the $i$-th query.

### Sample Input 1
```
3 3
1 1 2
8
1
2
3
4
5
6
7
8
```

### Sample Output 1
```
1
1
2
3
2
3
1
2
```

### Sample Explanation 1
Initially, $A=(1,1,2)$. With each operation, $A$ changes as follows:
First operation: The counts of $1,2,3$ in $A$ are $2,1,0$ respectively, so $v=3$. Append $v$ to the end of $A$, giving $A=(1,1,2,3)$.
Second operation: The counts of $1,2,3$ in $A$ are $2,1,1$ respectively, so $v=2$. Append $v$ to the end of $A$, giving $A=(1,1,2,3,2)$.
Third operation: The counts of $1,2,3$ in $A$ are $2,2,1$ respectively, so $v=3$. Append $v$ to the end of $A$, giving $A=(1,1,2,3,2,3)$.
$\vdots$
After performing the operation $10^{100}$ times, $A$ becomes $A=(1,1,2,3,2,3,1,2,\dots)$.

### Sample Input 2
```
7 30
20 26 3 14 4 4 9
10
31
9
21
23
97
99
30
79
57
3
```

### Sample Output 2
```
30
2
18
21
7
9
29
19
27
3
```

---

## 中文翻译
### 时间限制
2 秒 / 内存限制：1024 MiB
分值：475 分

### 问题描述
给定整数 $N,M$ 和一个长度为 $N$ 的整数序列 $A=(A_1,A_2,\dots,A_N)$，序列中每个元素的取值范围为 $[1,M]$。

对该序列 $A$ 执行 **$10^{100}$ 次** 如下操作：
设 $v$ 是 $[1,M]$ 中在 $A$ 内出现次数最少的整数；若有多个满足条件的数，选择其中**最小**的那个。将 $v$ 追加到序列 $A$ 的末尾。

给定 $Q$ 次询问，第 $i$ 次询问给出整数 $X_i$，请求出执行完所有操作后，序列 $A$ 中第 $X_i$ 个元素的值。

### 约束条件
- $1\le N,M\le 5\times 10^5$
- $1\le A_i \le M$
- $1\le Q\le 2\times 10^5$
- $1\le X_i \le 10^{18}$
- 所有输入值均为整数。

### 输入格式
输入从标准输入按以下格式读取：
```
N M
A_1 A_2 \dots A_N
Q
X_1
X_2
……
X_Q
```

### 输出格式
输出 $Q$ 行，第 $i$ 行输出第 $i$ 次询问的答案。

### 样例输入 1
```
3 3
1 1 2
8
1
2
3
4
5
6
7
8
```

### 样例输出 1
```
1
1
2
3
2
3
1
2
```

### 样例说明 1
初始序列 $A=(1,1,2)$，每次操作后序列变化如下：
第一次操作：$1,2,3$ 的出现次数分别为 $2,1,0$，因此 $v=3$，追加后序列变为 $(1,1,2,3)$。
第二次操作：$1,2,3$ 的出现次数分别为 $2,1,1$，因此 $v=2$，追加后序列变为 $(1,1,2,3,2)$。
第三次操作：$1,2,3$ 的出现次数分别为 $2,2,1$，因此 $v=3$，追加后序列变为 $(1,1,2,3,2,3)$。
……
执行 $10^{100}$ 次操作后，序列 $A$ 为 $(1,1,2,3,2,3,1,2,\dots)$。

### 样例输入 2
```
7 30
20 26 3 14 4 4 9
10
31
9
21
23
97
99
30
79
57
3
```

### 样例输出 2
```
30
2
18
21
7
9
29
19
27
3
```