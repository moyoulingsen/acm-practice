#include <bits/stdc++.h>
using namespace std;

typedef long long ll;  

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;cin>>t;
    int ans = 0;
    vector<int> arr()
    while(t--){
        
        
    }

    cout <<ans;

    return 0;
}